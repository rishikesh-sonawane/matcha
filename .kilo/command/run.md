---
description: Setup venv and run Matcha
---
Create the virtual environment if missing, install dependencies, and run the app:

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 main.py
```
