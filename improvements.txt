Here's the complete production-grade gap analysis across 17 categories:
---
Critical (blocks production)
Issue
No structured logging — console.print() everywhere, impossible to debug production issues
Zero scraper tests — no tests for LinkedIn, Indeed, Naukri, RemoteOK, SerpAPI, or web search scrapers
Zero profile/AI tests — ai_extract_profile, ai_generate_queries, ai_score_job untested
Zero orchestration tests — search_jobs, rank_jobs, prompt_loop untested
API keys in plaintext — serpapi_key and AI keys stored unencrypted in ~/.matcha/config.json
docker-compose.yml is broken — syntax error prevents building
High Severity
Error Handling
- Have we hardcoded the minimax environment variable? i mean we are using the minimax model api key but not everyone will.. right? so basically the application should work with any model’s api key..
- run_scraper() catches all Exception and silently returns [] — errors are lost (main.py:91-92)
- AI calls (_call_ai) catch all Exception and return None — no distinction between auth/network/rate-limit errors (ai.py:70-71)
- Bug: deduplicate() company comparison uses token_sort_ratio(norm_title, ...) instead of company when either company field is empty (main.py:115-118)
Testing
- No tests for actions.py, config.py, settings.py, scrapers/utils.py
- No integration or E2E tests
Configuration
- Two overlapping config systems (config.py JSON vs settings.py YAML) with no schema validation, no atomic writes, no versioning
Security
- Config file has no restricted permissions — any user/process can read API keys
- No OS keychain integration for credential storage
Monitoring
- No health check metrics, no scraper success/failure rates, no way to know if LinkedIn changed their HTML
Performance
- No scraper pagination — every scraper fetches only page 1, missing thousands of results
- deduplicate() is O(n²) with fuzzy string matching
Rate Limiting
- LinkedIn profile scraping bypasses the rate limiter (profile.py:246)
- DuckDuckGo scrapers (Indeed fallback, Naukri, Web Search) bypass the rate limiter
Portability
- Indeed hardcoded to India domain (in.indeed.com) — can't search US/UK markets
Medium Severity
- Duplicated stop words lists (2,000+ lines) across 4 files
- Pydantic models defined but never used — all code operates on raw dicts
- Recursive re-run (main() calling itself) will hit Python recursion limit
- No --export flag for CSV/JSON output
- No job application tracking UI (despite set_job_status existing in actions.py)
- No blacklist/filtering for companies, titles, or locations
- No salary/job type filtering
- No scheduled/cron mode or email digests
- No --version flag
- No --health CLI flag
- CI doesn't test on macOS, doesn't run type checking, no security scanning
Low Severity
- Several cosmetic and polish issues: no undo for save/unsave, no keyboard shortcuts for filters, no confirmation on re-run, no color scheme customization, no .editorconfig, no CONTRIBUTING.md, no LICENSE file
---
The top priorities to make this production-grade are logging, comprehensive testing, secure credential storage, scraper pagination, and fixing the silent error swallowing across the entire codebase.